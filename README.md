# BookBridge

A modern full-stack smart library portal with separate Administrator and Student workflows.

## Features

- Real signup, login, logout, and protected sessions
- Account roles: Administrator and Student
- Secure password hashing with bcrypt
- Signed HTTP-only session cookies
- Persistent SQLite database stored in `data/library.sqlite`
- Administrator dashboard for inventory, issue records, and student requests
- Student dashboard for browsing books and requesting access
- Request approval workflow similar to an offline library desk
- Due dates and late-fine calculation on returns

## Administrator Workflow

Administrators can:

- Add books with title and author details
- Edit or remove book records
- View all books and current issue status
- Directly issue books to a student name
- Return issued books and calculate fines
- Monitor student requests
- Approve a request, which automatically issues the book to that student
- Reject a request
- View access history for approved, rejected, and returned requests

## Student Workflow

Students can:

- Create a student account
- Login to a student-only interface
- Browse and search the book catalog
- Request an available book
- Track pending requests
- See approved/issued books and due dates

## Tech Stack

- React
- Vite
- Node.js
- Express
- sql.js SQLite database
- bcryptjs

## Project Structure

```text
deknek-library-website/
  client/src/          React frontend
  server/              Express backend
  server/db/           Database helper
  data/                Local SQLite database, created at runtime
  dist/                Production frontend build
  Main.java            Original CLI Java version
  LibraryGUI.java      Original Swing GUI Java version
```

## Run Locally

Install dependencies:

```bash
npm install
```

Create an environment file:

```bash
cp .env.example .env
```

Update `.env` and set a long random value:

```text
SESSION_SECRET=replace-this-with-a-long-random-secret
```

Run the frontend and backend together:

```bash
npm run dev
```

Open:

```text
http://localhost:5173
```

## Production Build

Build the React frontend:

```bash
npm run build
```

Start the Express server:

```bash
npm start
```

Open:

```text
http://localhost:4000
```

## API Routes

```text
POST /api/auth/signup
POST /api/auth/login
POST /api/auth/logout
GET  /api/auth/me

GET    /api/books
POST   /api/books
PUT    /api/books/:id
DELETE /api/books/:id
POST   /api/books/:id/issue
POST   /api/books/:id/return
POST   /api/books/:id/request

GET  /api/requests
POST /api/requests/:id/approve
POST /api/requests/:id/reject
```

## Deployment

This project can be deployed as one Node web service because Express serves both the API and the built React app.

[![Deploy to Render](https://render.com/images/deploy-to-render-button.svg)](https://render.com/deploy)

The repository includes `render.yaml`, so Render can create the service from the blueprint.

Blueprint settings:

```text
Build Command: npm install --include=dev && npm run build
Start Command: npm start
Environment:
  NODE_ENV=production
  SESSION_SECRET=<generated by Render>
Health Check Path: /api/health
Persistent Disk: /opt/render/project/src/data
```

Manual Render setup:

1. Push this repository to GitHub.
2. Open Render and create a new Web Service.
3. Connect the GitHub repository.
4. Use `main` as the branch.
5. Use the build and start commands shown above.
6. Add the persistent disk shown above so database data survives restarts.

## Original Java Version

The original Java CLI and Swing files are still included:

```bash
javac Main.java
java Main

javac LibraryGUI.java
java LibraryGUI
```
